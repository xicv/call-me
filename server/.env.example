# CallMe Server Configuration
# ==============================

# ===================
# Phone Provider
# ===================

# Choose: 'telnyx' (default, ~50% cheaper) or 'twilio'
PHONE_PROVIDER=telnyx

# For Telnyx (https://portal.telnyx.com):
#   PHONE_ACCOUNT_SID = Connection ID
#   PHONE_AUTH_TOKEN = API Key (v2)
#
# For Twilio (https://console.twilio.com):
#   PHONE_ACCOUNT_SID = Account SID
#   PHONE_AUTH_TOKEN = Auth Token

PHONE_ACCOUNT_SID=your_connection_id_or_account_sid
PHONE_AUTH_TOKEN=your_api_key_or_auth_token
PHONE_NUMBER=+1234567890

# Legacy Twilio vars also work: TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_PHONE_NUMBER

# ===================
# Speech-to-Text (STT)
# ===================

# Model: 'gpt-4o-mini-transcribe' (default, 50% cheaper) or 'whisper-1'
STT_MODEL=gpt-4o-mini-transcribe
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ===================
# Text-to-Speech (TTS)
# ===================

# Choose: 'chatterbox' (default, free self-hosted) or 'openai'
TTS_PROVIDER=chatterbox

# For Chatterbox: docker run -p 5100:5100 resemble-ai/chatterbox
CHATTERBOX_URL=http://localhost:5100

# Voice (works for both): alloy, echo, fable, onyx, nova, shimmer
TTS_VOICE=onyx

# ===================
# Server
# ===================

PUBLIC_URL=https://api.callme.dev
PORT=3333

# ===================
# Mode Selection
# ===================

# OPTION A: Self-Host Mode (single user, no payments)
# Just set your phone number - no database or Stripe needed
# SELF_HOST_PHONE=+1234567890
# SELF_HOST_API_KEY=my-secret-key  # Optional, defaults to "self-host"

# OPTION B: SaaS Mode (multi-user with subscriptions)
# Leave SELF_HOST_PHONE unset and configure Stripe below

# ===================
# SaaS Mode: Database
# ===================

DATABASE_PATH=./callme.db

# ===================
# SaaS Mode: Stripe Subscriptions
# ===================

# Get these from https://dashboard.stripe.com
STRIPE_SECRET_KEY=sk_live_xxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxx

# Create a subscription product in Stripe and get the Price ID
# e.g., $20/month recurring price
STRIPE_PRICE_ID=price_xxxxx

# ===================
# Subscription Plan Configuration
# ===================

# Monthly subscription price in cents ($20 = 2000)
MONTHLY_PRICE_CENTS=2000

# Minutes included per month
MONTHLY_MINUTES=60

# Price per additional credit minute in cents ($0.50 = 50)
CREDIT_PRICE_PER_MINUTE=50
